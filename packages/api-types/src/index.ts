// Shared API types for frontend + admin.
//
// TODO (first cleanup PR): move the response/DTO shapes that are currently
// duplicated in apps/frontend/src/types and apps/admin/src/api/types.ts here,
// then import them as `@huphup/api-types` from both apps.

export {};
